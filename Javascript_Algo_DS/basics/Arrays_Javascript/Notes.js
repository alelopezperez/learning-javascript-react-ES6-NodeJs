// Arrays in Javascript can have any types of variables like this:
//Array are not immutable
var arr = ["string", 1, 1.5]


// Shift in an array is to remove the first element.
var myArray = [["John", 23], ["dog", 3]];
var removedFromMyArray = myArray.shift();

// Push to add at the back
// Unshift to add at the front
// Pop
// shift

