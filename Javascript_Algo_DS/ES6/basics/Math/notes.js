function randomFraction() {

  return Math.random();

}

// Math Random returns a number a decimal number from 0(insclusive) to 1(exclusive)


Math.floor(Math.random()*10);
// Using floor and multiplication  to get whole number from O(inclusive) to the FACTOR(exclusive)

console.log(randomWholeNum());

// Math formula to get both min and max, inclusive

Math.floor(Math.random() * (max - min + 1)) + min


// Parse to int -takes in a string assumes is base 10 by defaultand casts it to number in ;

parseInt("1252")

// it can take a second arugemento for radix; to specify the radix(base) so it can transform it to a nubmer in base 10;

parseInt("11",2); // is a binary number and it returns 3;
